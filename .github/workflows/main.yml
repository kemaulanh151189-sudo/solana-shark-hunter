name: Solana Whale Scanner Pro

on:
  schedule:
    - cron: '*/2 * * * *' # Chạy mỗi 2 phút
  workflow_dispatch: # Cho phép nhấn nút chạy bằng tay

jobs:
  scanner:
    name: Market Analysis Run
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install requests

      # BƯỚC MỚI: ÉP GỬI THÔNG BÁO TEST MỖI KHI CHẠY
      - name: Test Telegram Connection
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          curl -s -X POST https://api.telegram.org/bot$TELEGRAM_TOKEN/sendMessage \
          -d chat_id=$TELEGRAM_CHAT_ID \
          -d text="🟢 **BOT STATUS:** Active & Hunting! (Nhịp quét mỗi 2 phút)" \
          -d parse_mode="Markdown"

      - name: Run Shark Hunter Bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          HELIUS_API_KEY: ${{ secrets.HELIUS_API_KEY }}
        run: python shark_hunter.py
